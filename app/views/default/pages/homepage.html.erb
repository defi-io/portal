<%= render 'default/shared/news_tab' %>

<ul class="title pt-4">
	<% @pages.each do |page| %>
		<li>
			<h2 class="list"><%=link_to page.title, page.materialized_path %></h2>
            <span class="text-gray-500"><%= page.published_at %></span>
		</li>
<% end %>
</ul>
			