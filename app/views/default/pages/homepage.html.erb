<%= render 'default/shared/news_tab' %>

<ul class="title pt-4" id="news_list">
	<%= render partial: 'news/list', locals: { pages: @pages } %>
</ul>

<%= render partial: 'news/page_link', locals: { page: @page } %>
